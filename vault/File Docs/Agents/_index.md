# Agents — Index

הגדרות שלושת הסוכנים המתמחים (sub-agents) שראובן מתזמר. כל קובץ הגדרה יושב
ב-`.claude/agents/` ומכיל frontmatter (שם, description, tools) + system prompt.

## Topics

- [[chen-agent]] — חן, חוקרת הרשת (WebSearch/WebFetch → Content/)
- [[yael-agent]] — יעל, כותבת התוכן (Content/ → Output/ MD+HTML)
- [[yuval-agent]] — יובל, מעצב התמונות (gpt-image-gen → yuval/outputs/)
